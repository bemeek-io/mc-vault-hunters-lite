plugins {
    // Auto-downloads the Java 25 toolchain when the local machine lacks it.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "VaultHuntersLite"
